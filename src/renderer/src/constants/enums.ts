export enum Role {
  Admin = 'Admin',
  Manager = 'Manager',
  Staff = 'Staff'
}

export enum UserStatus {
  Active = 'active',
  Inactive = 'inactive'
}

export enum OrderType {
  Purchase = 'Purchase',
  Sales = 'Sales'
}

export enum OrderStatus {
  Pending = 'Pending',
  Completed = 'Completed',
  Cancelled = 'Cancelled'
}

export enum SupplierStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE'
}
